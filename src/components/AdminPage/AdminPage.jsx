import React from "react";
import s from './AdminPage.module.css';

const AdminPage = (props) => {
    return (
        <div className={`${s.adminPage} ${"container"}`}>
            admin panel йбадь його нахуй блять
        </div>
    );
}

export default AdminPage;